apiVersion: 1

providers:
  - name: 'all'
    orgId: 1
    folder: ''
    type: file
    disableDeletion: false
    # how often Grafana will scan for changed dashboards
    updateIntervalSeconds: 30
    options:
      path: /dash
